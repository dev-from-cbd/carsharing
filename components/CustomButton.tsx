import React from "react";

const CustomButton = () => {
  return (
    <button
      disabled={false}
      type={"button"}
      className={`custom-btn`}
      onClick={}
    >
      CustomButton
    </button>
  );
};

export default CustomButton;
